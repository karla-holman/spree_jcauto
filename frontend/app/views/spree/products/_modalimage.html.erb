<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:large), :itemprop => "image", id: "product-modal-image", style: "width: 100%" %>
<% else %>
  <%= product_image(@product, :itemprop => "image", id: "product-modal-image", style: "width: 100%") %>
<% end %>
